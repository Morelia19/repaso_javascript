// ternary operator = un shortcut para escribir un sentencia if else
//                             Ayuda a asignar una bariable basada en una condición
//                             condición ? codigoSiVerdadero : codigoSiFalso;

let edad = 20;
let mensaje = edad >=18 ? "Eres mayor" : "Eres menor";
console.log(mensaje);

let hora = 9;
let saludo =hora > 12 ? "Buenas tardes" : "Buenas noches";
console.log(saludo);

let isEstudiante = true;
let mensaje2 = isEstudiante ? "Eres un estudiante" : "No eres un estudiante";
console.log(mensaje2);

